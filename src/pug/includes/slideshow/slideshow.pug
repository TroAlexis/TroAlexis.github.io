mixin slideshow(slidesUrls)
    .slideshow
        each val in slidesUrls
            .slideshow__slide
                img(src=val)
        a.slideshow__prev
        a.slideshow__next
        div.slideshow__dots
            each val, index in slidesUrls
                span.slideshow__dot(data-index=index)