// inf {text, href, submit}
mixin btn(options)
    if options.submit
        button.btn(type='submit')&attributes(attributes)= options.text
    else
        a.btn(href=options.href ? options.href : '#')&attributes(attributes)= options.text