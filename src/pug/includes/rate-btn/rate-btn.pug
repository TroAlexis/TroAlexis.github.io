// inf {rating: num}
mixin rate-btn(options)
    .rate-btn&attributes(attributes)
        -var i = 1
        while i <= 5
            a(href='#' class='rate-btn__star', data-index=i++)(class = i <= options.rating ? 'active' : '')